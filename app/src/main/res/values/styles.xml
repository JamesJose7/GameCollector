<resources>

    <style name="BaseBottomSheetModalStyle" parent="Widget.Design.BottomSheet.Modal">
        <item name="android:background">@drawable/bottom_sheet_background</item>
    </style>
    
    <style name="BaseToggleButton" parent="android:Widget.Button.Toggle">
        <item name="android:background">@drawable/custom_toggle_button_background</item>
        <item name="android:paddingVertical">8dp</item>
        <item name="android:paddingHorizontal">10dp</item>
        <item name="android:textColor">@color/custom_toggle_text_color</item>
        <item name="android:minHeight">0dp</item>
        <item name="android:minWidth">0dp</item>
    </style>

</resources>
